<h1>User Details</h1>

<div class="card">
  <div class="card-body">
    <h5 class="card-title">Email</h5>
    <p class="card-text"><%= @user.email %></p>
    
    <%= link_to 'Add New', new_user_path, class: 'btn btn-secondary' %>
  </div>
</div>
